schema_version: "1.0"

study_suite:
  name: history_modes_v1
  environment: configs/environments/volcengine.yaml
  output_root: /vepfs-mlp2/queue010/20262202597/math_flywheel/runs
  allow_missing_artifacts: false
  allow_dirty_worktree: false
  # These are the only non-study fields allowed to differ in this suite.
  method_specific_fields:
    - adaptive_sampling.history_mode
    - adaptive_sampling.decay_lambda
  methods:
    - full_history_cumulative
    - full_history_cycle_reset
    - full_history_time_decay
  seeds:
    - 42
    - 123
    - 2026
  models:
    - name: qwen2.5-7b-instruct
      path: /vepfs-mlp2/queue010/20262202597/Qwen2.5-7B-Instruct
  budgets:
    - 1350
